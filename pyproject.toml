[project]
name = "kiro-shmup"
version = "0.1.0"
description = "Arcade side-scrolling shmup game with Kiro"
requires-python = ">=3.10"
dependencies = [
    "pygame>=2.5.0",
]

[tool.uv]
default-groups = ["dev"]

[dependency-groups]
dev = [
    "pytest>=7.4.0",
]
